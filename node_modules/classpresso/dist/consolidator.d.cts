import { b as ConsolidationCandidate, c as ClassMapping, O as OptimizationMetrics, a as ClasspressoConfig, M as MappingManifest } from './index-aEKm6WOP.cjs';

/**
 * Consolidator - Creates consolidated class mappings
 */

/**
 * Create class mappings from consolidation candidates
 */
declare function createClassMappings(candidates: ConsolidationCandidate[]): ClassMapping[];
/**
 * Build a lookup map from original class patterns to their consolidated names
 */
declare function buildReplacementMap(mappings: ClassMapping[]): Map<string, {
    consolidated: string;
    excludedClasses: string[];
}>;
/**
 * Create and save the mapping manifest
 */
declare function saveMappingManifest(mappings: ClassMapping[], metrics: OptimizationMetrics, config: ClasspressoConfig): Promise<string>;
/**
 * Load an existing manifest
 */
declare function loadMappingManifest(buildDir: string): Promise<MappingManifest | null>;

export { buildReplacementMap, createClassMappings, loadMappingManifest, saveMappingManifest };
